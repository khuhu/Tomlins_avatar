#!/usr/bin/env Rscript

###This script is used for querying mysql data and then parsing through it to get name identifiers for uniq samples
###Non-unique will be any runs will be anything where fields of SAMPLE, BED, and possibly REPORTNUM is the same


